#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 if successful
 */
void print_alphabet(void);

int main(void)
{
	print_alphabet(void);
    	return (0);
}
void print_alphabet(void)
{
	char c;

	a = 'a';

	while (a <= 'z')
	{
		_putchar(c);
		c++;
	}
}
